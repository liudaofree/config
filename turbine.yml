server:
  port: 8763

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://192.168.24.231:8761/eureka/

management:
  security:
    enabled: false

turbine:
  appConfig: gate
  clusterNameExpression: new String("default")
